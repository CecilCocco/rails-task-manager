<h1>Tasks</h1>
<% @tasks.each do |task| %>
  <p><%= link_to task.title, task_path(task) %></p>
<% end %>

<button><%= link_to "New tasks", tasks_new_path %></button>

<%= link_to "Delete",
  task_path(@task),
  data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}%>
